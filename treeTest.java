import org.junit.Test;
import static org.junit.Assert.*;

public class treeTest {
	@Test
	public void tree_can_be_initialized_with_root_bt_default_null() {
		tree tree = new tree();
		assertEquals(tree.root,null);
	}

	// @test
}